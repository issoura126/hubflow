# دليل التحديث السريع - N8N Pro v4.1

## 🎯 الإصلاحات المطبقة

### ✅ إصلاح #1: تحريك العقد (Drag & Drop)
**المشكلة:** العقد لا تتحرك بشكل صحيح
**الحل:** تحديث نظام السحب والإفلات بالكامل

**كيفية الاختبار:**
1. افتح التطبيق
2. أضف عقدة جديدة (اضغط على أي عقدة من القائمة اليسرى)
3. اسحب العقدة من الـ **header** (الجزء العلوي الأزرق)
4. يجب أن تتحرك بسلاسة ✓

---

### ✅ إصلاح #2: تعديل عقد الذكاء الاصطناعي
**المشكلة:** عدم القدرة على اختيار خيارات متعددة
**الحل:** إضافة دعم كامل لحقول multiselect

**كيفية الاختبار:**
1. أضف عقدة "AI Text Analyzer" 🔍
2. اضغط على زر "Settings" ⚙️
3. في "Analysis Type" ستجد checkboxes
4. اختر عدة options (مثل: sentiment, summary, keywords)
5. اضغط "Save"
6. أعد فتح Settings - يجب أن تكون اختياراتك محفوظة ✓

---

## 🚀 كيفية التثبيت

### الطريقة 1: تحديث الملف فقط
```bash
# استبدل الملف القديم
cp n8n-pro-enhanced-v4.1-FIXED/assets/js/app.js /path/to/your/n8n/assets/js/app.js
```

### الطريقة 2: تثبيت كامل
```bash
# فك الضغط
unzip n8n-pro-enhanced-v4.1-FIXED.zip

# انسخ المجلد إلى مسار التثبيت
cp -r n8n-pro-enhanced-fixed/* /var/www/html/n8n/
```

---

## 🔍 نقاط الاختبار الرئيسية

### اختبار السحب والإفلات:
- [ ] إضافة 3 عقد مختلفة
- [ ] سحب كل عقدة إلى مواقع مختلفة
- [ ] التحقق من تحديث الخطوط بين العقد
- [ ] سحب عقدة فوق عقدة أخرى (يجب أن تظهر فوقها)

### اختبار عقد الذكاء الاصطناعي:
- [ ] فتح إعدادات AI Text Analyzer
- [ ] اختيار 3+ options من Analysis Type
- [ ] حفظ وإعادة فتح للتحقق
- [ ] تجربة تنفيذ workflow (يتطلب Ollama)

---

## 📋 متطلبات التشغيل

### للاستخدام الكامل:
- PHP 7.4+
- MySQL/MariaDB
- خادم ويب (Apache/Nginx)
- **Ollama** (لعقد الذكاء الاصطناعي)

### لتثبيت Ollama:
```bash
# Linux/Mac
curl -fsSL https://ollama.com/install.sh | sh

# تحميل نموذج
ollama pull llama3.2

# تشغيل الخدمة
ollama serve
```

---

## 🐛 حل المشاكل

### المشكلة: العقد لا تزال لا تتحرك
**الحل:**
1. امسح cache المتصفح (Ctrl+Shift+Delete)
2. أعد تحميل الصفحة (Ctrl+Shift+R)
3. تحقق من console (F12) للأخطاء

### المشكلة: Multiselect لا يظهر
**الحل:**
1. تأكد من استبدال ملف app.js بالكامل
2. تحقق من أن حجم الملف ~1049 سطر
3. افتح console وابحث عن أخطاء JavaScript

### المشكلة: عقد AI لا تعمل
**الحل:**
1. تأكد من تشغيل Ollama: `ollama serve`
2. تحقق من أن المنفذ 11434 مفتوح
3. جرب: `curl http://localhost:11434/api/tags`

---

## 📝 الميزات الإضافية

### اختصارات لوحة المفاتيح:
- `Ctrl+S` - حفظ workflow
- `Ctrl+Enter` - تنفيذ workflow
- `Delete` - حذف العقدة المحددة
- `Escape` - إلغاء وضع الربط

### نصائح الاستخدام:
1. **اسحب من الـ header فقط** لتجنب الضغط على الأزرار
2. استخدم **Auto Arrange** لترتيب العقد تلقائياً
3. اضغط على **الخط** بين عقدتين لحذف الربط
4. استخدم **Zoom** للتكبير/التصغير

---

## 📊 قائمة التحقق الكاملة

### قبل الاستخدام:
- [ ] فك ضغط الملف
- [ ] التحقق من متطلبات PHP و MySQL
- [ ] تثبيت Ollama (إذا كنت ستستخدم AI nodes)
- [ ] نسخ الملفات إلى الخادم

### بعد التثبيت:
- [ ] فتح التطبيق في المتصفح
- [ ] اختبار إضافة عقدة
- [ ] اختبار سحب وإفلات
- [ ] اختبار حفظ workflow
- [ ] اختبار تنفيذ workflow بسيط

### اختبار عقد AI:
- [ ] إضافة AI Text Analyzer
- [ ] تكوين multiselect options
- [ ] حفظ التكوين
- [ ] تنفيذ تحليل نص بسيط

---

## 🎉 ماذا بعد؟

بعد التأكد من عمل كل شيء:
1. أنشئ workflow أول
2. جرب ربط عدة عقد معاً
3. استكشف عقد الذكاء الاصطناعي المختلفة
4. احفظ workflows للاستخدام لاحقاً

---

## 💬 الدعم

إذا واجهت أي مشاكل:
1. افتح console المتصفح (F12)
2. التقط screenshot للخطأ
3. تحقق من logs في مجلد `logs/`
4. راجع ملف `FIXES_APPLIED.md` للتفاصيل

---

**جاهز للاستخدام! 🚀**

تم إصلاح جميع المشاكل المذكورة بدقة ودقة عالية.
